# Hiyuki-Bot

Base inicial del bot WhatsApp con Baileys.

## Comandos base
- `.menu`
- `.update`

## Inicio rapido
1. `cp .env.example .env`
2. Completa `OWNER_NUMBER` y `BOT_NUMBER`
3. `npm install`
4. `npm start`
